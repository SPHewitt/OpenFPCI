# WARNING: Currently requires functionObject to
# be run prior to this being called

cutPlane
{
   type cuttingPlanesFunc;
   functionObjectLibs
   (
     "libcuttingPlanesFunc.so"
   );

   startTime 0.5;
   point (0 3 0);
   dir (0 1 0);
   name yplane;
}

